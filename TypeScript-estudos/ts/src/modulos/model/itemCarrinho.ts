//para que fique acessivel para outro modulo -> export
//no arquivo que deseja importar -> import

export default interface ItemCarrinho {
    nome: string;
    preco: number;
    quantidade: number;
}

