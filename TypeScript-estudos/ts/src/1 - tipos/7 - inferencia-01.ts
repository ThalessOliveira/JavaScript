let valor = 30;
console.log(valor);

//quando não é declarado um tipo específico na variável, ela assume o tipo do primeiro valor passado, no caso do exemplo, number

//valor = 'João';
console.log(valor);

//se tentamos passar outro tipo dentro da variável, o código e a execução assumem erro