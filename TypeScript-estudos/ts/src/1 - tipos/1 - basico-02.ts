let nome: string = 'João';
let numeros: number[] = [1, 2, 3, 4];

console.log(nome);
console.log(numeros);

//se criar variaveis iguais em dois arquivos, ele vai entender como erro

export {};
// com o export{}, isso para, pois ele entende que isso faz parte de um módulo