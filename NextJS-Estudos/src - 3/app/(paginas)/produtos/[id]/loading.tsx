import Carregando from "@/app/components/shared/Carregando";

export default function PaginaCarregando(){
    return <Carregando quantidade={1} /> //{1} porque estara carregando um produto em especifico
}